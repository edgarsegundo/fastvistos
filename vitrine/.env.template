# Django settings — obrigatório
ALLOWED_HOSTS=127.0.0.1,localhost,0.0.0.0
DEBUG=False
SECRET_KEY=

# Cloudflare R2 (S3-compatible object storage)
# Valores reais vêm do seu dashboard R2 / API token; deixe vazio se não usar R2
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_ENDPOINT_URL=https://<your-account-id>.r2.cloudflarestorage.com
R2_CUSTOM_DOMAIN=cdn.example.com

# Busca de imagens stock (aba "Stock" do seletor de imagem) — chaves
# PRÓPRIAS do vitrine, não reaproveitar as do msitesapp. Vazio desabilita
# só aquela fonte.
PEXELS_API_KEY=
PIXABAY_API_KEY=
UNSPLASH_ACCESS_KEY=

---
// CallToAction.astro
---


<div class="flex justify-center my-8">
  <button
    class="relative px-16 py-8 font-extrabold text-white text-3xl bg-gradient-to-r from-blue-600 via-cyan-400 to-green-400 rounded-full shadow-2xl overflow-hidden focus:outline-none focus:ring-4 focus:ring-blue-400 transition-all duration-300 group"
    style="font-size:2rem; min-width:320px;"
  >
    <span class="absolute inset-0 bg-gradient-to-r from-green-400 via-cyan-400 to-blue-600 opacity-0 group-hover:opacity-100 transition duration-500 blur-sm"></span>
    <span class="relative z-10 animate-pulse group-hover:animate-none">Comece Já</span>
    <span class="absolute left-0 top-0 w-full h-full rounded-full border-4 border-white opacity-0 group-hover:opacity-100 animate-spin-slow"></span>
  </button>
</div>

<style>
@keyframes spin-slow {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}
.animate-spin-slow {
  animation: spin-slow 2s linear infinite;
}
</style>

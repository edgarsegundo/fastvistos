---
// Import our compiled Tailwind CSS
import '../styles/global.css';
import Navigation from '../components/Navigation.astro';

export interface Props {
  title?: string;
  description?: string;
  keywords?: string;
  ogImage?: string;
  ogType?: string;
  noindex?: boolean;
  canonicalUrl?: string;
  structuredData?: any;
  bodyClass?: string;
}

const {
  title = "FastVistos - Especialistas em Vistos Americanos",
  description = "Assessoria completa para vistos americanos B1/B2, eTA do Canadá e vistos mexicanos. Aprovação garantida com nossa equipe especializada.",
  keywords = "visto americano, assessoria visa, consultoria visto, B1 B2, eTA Canada",
  ogImage = "/logo.png",
  ogType = 'website',
  noindex = false,
  canonicalUrl,
  structuredData,
  bodyClass = ''
} = Astro.props;

const currentUrl = canonicalUrl || Astro.url.href;
const siteUrl = "https://fastvistos.com.br";
---


  
</head>
<body
    class={`leading-normal tracking-normal text-white gradient ${bodyClass} antialiased`}
    style="font-family: 'Source Sans Pro', sans-serif;"
>
    <!-- Google Tag Manager (noscript) -->
    <noscript
        ><iframe
            src="https://www.googletagmanager.com/ns.html?id=GTM-59SRNCQD"
            height="0"
            width="0"
            style="display:none;visibility:hidden"></iframe></noscript
    >
    <!-- End Google Tag Manager (noscript) -->

  <!-- Navigation -->
  <Navigation />

  <!-- Main content -->
  <main>
    <slot />
  </main>
</body>
</html>

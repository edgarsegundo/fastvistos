---
/**
 * 🚨 AUTO-GENERATED FILE. DO NOT EDIT.
 * This file is generated by sync-blog.js and will be overwritten.
 * To customize, edit the template.
 */

// Shared Home Layout - Used across all sites
import '../styles/global.css';
import { siteConfig } from '../site-config.ts';
import HeadBasics from '../components/HeadBasics.astro';
import AnalyticsHead from '../components/AnalyticsHead.astro';
import AnalyticsBody from '../components/AnalyticsBody.astro';
import ThemeToggle from '../components/ThemeToggle.astro';

// Use full language tag (pt-BR) or just language (pt) based on site config
const htmlLang = siteConfig.site.useFullLanguageTag
    ? siteConfig.site.language
    : siteConfig.site.language.split('-')[0];

const {
    bodyClass = '',
    bodyStyle = '',
    themeToggleDisable = false, // New prop to control theme toggle visibility

    canonicalConf,
    faviconPathFromConf,
    seoFromConf,
    titleFromConf,
    descriptionFromConf,
    authorNameFromConf,
    imageFromConf,
    imageUrlFromConf,
    imageCaptionFromConf,
    imageWidthFromConf,
    imageHeightFromConf,

    pubDate,
    updatedDate,
} = Astro.props;
---

<!doctype html>
<html lang={htmlLang}>
    <head>
        <!-- Essential head elements (charset, viewport, etc.) MUST be first -->
        <HeadBasics
            generator={Astro.generator}
            noIndex={false}
            enhancedIndexing={false}
            canonical={canonicalConf}
            faviconPath={faviconPathFromConf}
            seo={seoFromConf}
            title={titleFromConf}
            description={descriptionFromConf}
            authorName={authorNameFromConf}
            image={imageFromConf}
            imageUrl={imageUrlFromConf}
            imageCaption={imageCaptionFromConf}
            imageWidth={imageWidthFromConf}
            imageHeight={imageHeightFromConf}
            pubDate={pubDate}
            updatedDate={updatedDate}
        />

        <!-- Analytics Scripts -->
        <AnalyticsHead siteConfig={siteConfig} />

        <!-- Fonts & Styles -->
        <link rel="preconnect" href={`https://${siteConfig.site.domain}`} />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:400,700&display=swap"
            rel="stylesheet"
        />

        <slot name="head" />
    </head>
    <body class={bodyClass} style={bodyStyle}>
        <!-- Analytics Body Tags -->
        <AnalyticsBody siteConfig={siteConfig} />

        <!-- Theme Toggle (conditional) -->
        {!themeToggleDisable && <ThemeToggle />}

        <!-- Body content -->
        <slot />
    </body><script src="../lib/updatable-editor.js"></script>
</html>
